# Must match the path segment you share with hosts (see README).
# Dev default is "dev" -> http://localhost:3000/setup/dev
REACT_APP_ADMIN_SETUP_PATH=dev

# Optional: override API base (default same-origin /api).
# REACT_APP_API_BASE=/api
